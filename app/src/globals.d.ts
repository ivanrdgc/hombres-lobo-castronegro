/** Sello de versión inyectado por Vite en build (ver vite.config.ts). */
declare const __APP_VERSION__: string;
/** Id único de este build (epoch ms). Se compara con /version.json para
 *  recargar a todos cuando se despliega una versión nueva. */
declare const __BUILD_ID__: string;
